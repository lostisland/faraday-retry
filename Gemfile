# frozen_string_literal: true

source 'https://rubygems.org'

gemspec

gem 'base64', '~> 0.2' # Avoid a RuboCop warning about bae64 no longer being shipped with later Ruby versions.

group :test do
  gem 'faraday-multipart', '~> 1.0'
end
